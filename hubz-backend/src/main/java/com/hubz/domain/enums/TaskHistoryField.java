package com.hubz.domain.enums;

/**
 * Enum representing the fields that can be tracked in task history.
 */
public enum TaskHistoryField {
    TITLE,
    DESCRIPTION,
    STATUS,
    PRIORITY,
    ASSIGNEE,
    DUE_DATE,
    GOAL
}
